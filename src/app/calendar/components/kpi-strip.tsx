/**
 * The KPI strip rendered above the owner calendar is the shared
 * `KpiStrip` component. Re-exporting it here keeps the calendar page's
 * imports symmetrical with the other locally-extracted components.
 */
export { KpiStrip } from "@/interface/components/kpi-strip";
